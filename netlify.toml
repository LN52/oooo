[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # 这些环境变量是预期的，用于客户端代码
  # Supabase anon key 是设计为可以在客户端使用的
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

